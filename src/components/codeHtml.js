export const codeHtml = (element) => {
if (elemen == num) {
return `<option value="cp08_document|lk|${element}">Identificación como ${element}</option>`;
}
return `<option value="cp08_nombre|lk|${element}">Nombre ${element}</option>`;
};
